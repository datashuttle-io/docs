# WebSocket Events

Connect to `ws://host:8080/ws/pipelines` for real-time pipeline events.

Events: `pipeline.created`, `pipeline.paused`, `pipeline.resumed`, `pipeline.dropped`, `pipeline.commit`, `pipeline.error`
